
SET(PREFIXES
  dti
  fsl
  scalar
  tsa
)

FOREACH(prefix ${PREFIXES})
  FILE(GLOB_RECURSE SCRIPT_FILES ${prefix}*)
  INSTALL(FILES ${SCRIPT_FILES} DESTINATION scripts PERMISSIONS OWNER_READ OWNER_EXECUTE GROUP_READ GROUP_EXECUTE WORLD_READ WORLD_EXECUTE COMPONENT scripts )
ENDFOREACH(prefix ${PREFIXES})

